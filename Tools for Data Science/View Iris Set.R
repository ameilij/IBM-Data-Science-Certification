library(datasets)
data(iris)
View(iris)

# Now let's find how many different species there are present in the 
# data set. Type the following command into the editor window and click 
# the run.
unique(iris$Species)

